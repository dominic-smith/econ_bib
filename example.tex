\documentclass{article}
\usepackage{url}
\usepackage{natbib}

\bibliographystyle{dominic}
\setcitestyle{authoryear, open={((},close={)}}

%% These lines don't actually matter, but I think it makes the URLs look nicer
\usepackage[hidelinks, pdftex]{hyperref}
\usepackage{xcolor}
\hypersetup{
    colorlinks,
    linkcolor={red!50!black},
    citecolor={blue!30!black},
    urlcolor={blue!90!black}
}
% End lines that don't matter


\begin{document}

This is a data citation \citep{CRT} and this is a published data citation \cite{Jarmin2002}.

\bibliography{example}

\end{document}


